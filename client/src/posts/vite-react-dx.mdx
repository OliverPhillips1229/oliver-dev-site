export const meta = {
  title: 'Vite + React: tiny DX wins',
  date: '2025-08-13',
  excerpt: 'A few small tweaks that make Vite projects nicer to work in.',
  tags: ['vite','react','dx']
}

## Handy dev scripts

- `npm run dev` at the repo root runs client + server together with hot reload.
- Add `--open` to auto-open the browser.
