{
  "name": "oliver-dev-server",
  "version": "1.0.0",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "npm --prefix ../client ci && npm --prefix ../client run build"
  },
  "dependencies": {
    "express": "^4.19.2",
    "path": "^0.12.7"
  }
}